apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "tabbyapi.fullname" . }}-models

spec:
  accessModes:
    {{- toYaml .Values.persistence.accessModes | nindent 4 }}

  resources:
    requests:
      storage: {{ .Values.persistence.size }}
