apiVersion: v1
kind: Service
metadata:
  name: {{ include "tabbyapi.fullname" . }}
spec:
  selector:
    app: {{ include "tabbyapi.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
