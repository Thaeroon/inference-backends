platform:
  vllm:
    repository: vllm/vllm-openai-rocm
    tag: v0.29.0

  gpu:
    enabled: true
    type: amd.com/gpu

  runtimeClassName: ""

  router:
    enabled: true

ingressClassName: traefik

ingresses:
  - name: vllm
    host: vllm.home.arpa
    paths:
      - path: /
        service:
          name: vllm-router-service
          port: 80
